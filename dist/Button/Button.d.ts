import React from "react";
import { ButtonProps } from "./ButtonProps";
export declare const Button: ({ className, type, onClick, onHover, children, primary, secondary, }: ButtonProps) => React.JSX.Element;
